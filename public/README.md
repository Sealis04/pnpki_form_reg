# `public/`

Drop the PNPKI subscriber PDF here as `form.pdf` to have the app auto-load it on
page open. Any PDF with fillable AcroForm boxes works.

The file is served at `${basePath}/form.pdf`, i.e.
`https://<user>.github.io/pnpki_form_reg/form.pdf` once deployed.
